// Package flow defines the capture data model: a Flow is one HTTP exchange,
// WebSocket session, or opaque tunnel seen by the proxy, plus the events the
// engine publishes as a flow progresses.
package flow
