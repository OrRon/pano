// Package api holds the request/response types of pano's control API. Both the
// CLI and the MCP server are thin clients of this API, so these types are the
// single source of truth for what pano can be asked to do.
package api
