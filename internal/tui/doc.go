// Package tui implements `pano ui`, an interactive terminal interface over the
// control API: a live flow list, a detail pane with the same body views the
// CLI and MCP server expose, LLM explain, diff, replay, rules and held
// requests. Like every other front end it is a thin client of the daemon.
package tui
