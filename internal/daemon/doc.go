// Package daemon is pano's composition root: it wires the certificate
// authority, the proxy engine, capture storage, the rules engine, the
// system-proxy manager and the control/MCP servers together, and implements
// the control API backend on top of them.
package daemon
